# 昼学シフトシステム 管理者マニュアル

## 1. システムの全体像

このシステムは、昼学シフトの希望調査、シフト作成、シフト表公開を行うためのシステムです。

運用は大きく3つに分かれます。

- 管理者用ローカルアプリ
  - 管理者が自分のPCで起動して使う画面です。
  - 名簿管理、希望調査の公開設定、回答確認、シフト作成、オンライン同期を行います。
- Supabase
  - オンライン上のデータ保存場所です。
  - ユーザーが送信した希望回答が保存されます。
  - 管理者はローカルアプリから回答を取り込みます。
- Netlify
  - ユーザー向けページを公開する場所です。
  - ユーザーはNetlifyのURLから希望回答やシフト表確認を行います。

基本的な考え方は、**管理者はローカルPCで作業し、ユーザーは公開URLから回答・閲覧する**という形です。

## 1.1 運営用アカウント

このシステムのSupabaseとNetlifyは、個人アカウントではなく運営用メールアドレスで管理します。

```text
hirugaku.shift@gmail.com
```

このメールアドレスは、昼学シフトシステムの運営・引き継ぎ用アカウントです。
Supabase、Netlify、その他このシステムに関係する外部サービスは、原則としてこのメールアドレスで管理してください。

注意点：

- 個人のGmailや大学個人メールで新しいSupabase/Netlifyプロジェクトを作らないでください。
- 管理者が交代する場合は、この運営用メールアドレスのログイン情報を安全な方法で引き継いでください。
- 運営用メールのパスワード、復旧方法、2段階認証の復旧コードは、管理者以外に共有しないでください。
- 古い個人アカウントで作成したNetlifyサイトは削除済みです。今後は運営用メールで作成したNetlifyサイトだけを使ってください。

## 2. 起動方法

1. 管理者PCで `hirugaku-shift` フォルダを開きます。
2. `run.bat` をダブルクリックします。
3. サーバーが起動します。
4. ブラウザで管理者ページを開きます。

通常は以下のURLを使います。

```text
http://127.0.0.1:8000/admin
```

ユーザー側のローカル確認ページは以下です。

```text
http://127.0.0.1:8000/
```

注意点：

- `run.bat` を起動している間だけ、管理者ページを使えます。
- PCを再起動した後は、再度 `run.bat` を起動してください。
- Netlifyで公開済みのユーザー向けページは、管理者PCを起動していなくてもアクセスできます。

## 3. 管理者ページの開き方

1. `run.bat` を起動します。
2. ブラウザで以下を開きます。

```text
http://127.0.0.1:8000/admin
```

3. 管理者パスワードを入力します。
4. 管理者ホームが表示されます。

管理者ホームには主に以下の項目があります。

- 希望調査・シフト表の公開設定
- 回答確認
- シフト作成
- 過去シフト入力
- 名簿管理
- オンライン同期

## 4. 公開情報の作成・編集

公開情報は、「何年何月の希望調査・シフト表を扱うか」を設定する画面です。

操作場所：

```text
管理者ページ → 希望調査・シフト表の公開設定
```

ここで設定する内容：

- 対象年
- 対象月
- 締め切り日
- シフト日時
- 希望調査の状態
- シフト表の公開状態

希望調査の状態は以下の3種類です。

- 非公開
  - ユーザー側には表示されません。
- 回答受付中
  - ユーザーが希望回答できます。
- 回答締切
  - ユーザー側に表示はされますが、回答受付は終了した状態です。

シフト表の公開状態は以下の2種類です。

- 非公開
  - ユーザー側ではシフト表を確認できません。
- 公開
  - ユーザー側でシフト表を確認できます。

## 5. 名簿の管理

操作場所：

```text
管理者ページ → 名簿管理
```

できること：

- メンバーの追加
- メンバー名の編集
- 車出し可否の変更
- メンバーの削除

車出し可能なメンバーは、シフト表上で名前の横にハンドルマークが表示されます。

注意点：

- メンバーを削除しても、内部的には完全削除ではなく非表示扱いになります。
- 過去のシフト記録との整合性を保つため、基本的にはこの仕様で問題ありません。
- 一度削除したメンバーを同じ名前で再追加すると、再び有効化されます。

## 6. 希望調査を公開する流れ

1. 管理者ページを開きます。
2. `希望調査・シフト表の公開設定` を開きます。
3. 対象年・対象月を設定します。
4. 締め切り日を設定します。
5. シフト日時をカレンダーから選択します。
6. 希望調査の状態を `回答受付中` にします。
7. 保存します。
8. `オンライン同期` を開きます。
9. `公開データを送る` を押します。
10. 必要に応じて `公開用ファイルを作成` を押します。
11. Netlifyに `public` フォルダをアップロードします。

この操作により、ユーザー側の公開ページに希望調査が表示されます。

## 7. Netlifyへpublicファイルをアップロードする流れ

ユーザー向けページの見た目や設定を更新した場合、Netlifyへ `public` フォルダをアップロードします。

操作の流れ：

1. 管理者ページを開きます。
2. `オンライン同期` を開きます。
3. `公開用ファイルを作成` を押します。
4. 作成された `public` フォルダを確認します。
5. Netlifyを開きます。
6. `hirugaku.shift@gmail.com` で管理している対象プロジェクトを開きます。
7. `Production deploys` 付近のアップロード欄に `public` フォルダをドラッグアンドドロップします。
8. デプロイ完了後、公開URLを開いて表示を確認します。

注意点：

- `public` フォルダの中身ではなく、`public` フォルダ自体をアップロードします。
- アップロード後、反映に少し時間がかかる場合があります。
- ユーザー向けページのURLはNetlify側で確認できます。
- Netlifyは必ず運営用メールアドレス `hirugaku.shift@gmail.com` のアカウントで操作してください。

## 8. ユーザー回答を取り込む流れ

ユーザーがNetlifyの公開ページから回答すると、その回答はSupabaseに保存されます。
管理者ページで確認するには、回答をローカルアプリに取り込む必要があります。

操作の流れ：

1. 管理者ページを開きます。
2. `オンライン同期` を開きます。
3. `回答を取り込む` を押します。
4. `回答を取り込みました` の表示を確認します。
5. 管理者ホームへ戻ります。
6. `回答確認` を開きます。
7. 回答内容を確認します。

注意点：

- 回答確認に最新回答が出ない場合は、まず `回答を取り込む` を実行してください。
- ユーザーが再回答した場合も、再度取り込むことで最新内容が反映されます。

## 9. シフト作成と保存

操作場所：

```text
管理者ページ → シフト作成
```

操作の流れ：

1. 対象月を選択します。
2. 編集する日付を選択します。
3. 左側に、その日に参加可能なメンバーが表示されます。
4. 右側の各会場・各枠にメンバーを配置します。
5. 必要に応じて、メンバーごとの過去会場回数や月内回数を確認します。
6. 配置が完了したら `保存する` を押します。
7. 保存後、シフト回数のカウントが更新されます。

注意点：

- 同じ日に同じメンバーを複数枠へ配置することはできません。
- `△` のメンバーも候補に表示されますが、注意ラベル付きで表示されます。
- 車出し可能なメンバーにはハンドルマークが表示されます。
- 保存しないまま別の日付へ移動すると、変更が反映されない可能性があります。

## 10. シフト表を公開する流れ

1. 管理者ページを開きます。
2. `シフト作成` で対象月のシフトを作成・保存します。
3. `希望調査・シフト表の公開設定` を開きます。
4. 対象月の `シフト表` を `公開` にします。
5. 保存します。
6. `オンライン同期` を開きます。
7. `公開データを送る` を押します。
8. 必要に応じて `公開用ファイルを作成` を押します。
9. Netlifyへ `public` フォルダをアップロードします。
10. ユーザー側の公開URLからシフト表が見えるか確認します。

注意点：

- ローカルでシフト表を公開にしただけでは、ユーザー側には反映されません。
- ユーザー側へ反映するには、`公開データを送る` が必要です。
- 画面や公開設定ファイルを変更した場合は、Netlifyへの再アップロードも必要です。

## 11. Supabase / Netlify のアカウント情報の管理方法

SupabaseとNetlifyは、ユーザー向け公開運用に必要です。
どちらも運営用メールアドレスで管理します。

```text
hirugaku.shift@gmail.com
```

個人アカウントで管理すると、管理者交代時にログインできなくなったり、公開ページを更新できなくなったりする可能性があります。
そのため、SupabaseとNetlifyは原則としてこの運営用メールアドレスでログインしてください。

Supabaseで管理するもの：

- ユーザー回答
- 公開用の名簿情報
- 希望調査情報
- シフト表情報

Netlifyで管理するもの：

- ユーザー向けWebページ
- 公開URL
- `public` フォルダのデプロイ

引き継ぎ時には、以下の情報を安全な方法で渡してください。

- 運営用メールアドレス
- 運営用メールアドレスのログイン情報
- Supabaseのログイン情報
- Netlifyのログイン情報
- Supabase Project URL
- Supabase anon public key
- Supabase service_role key
- Netlifyの公開URL
- 管理者ページのパスワード
- 運営用メールの復旧方法
- 2段階認証の復旧コード

注意点：

- パスワードやkeyをLINE、メール、共有ドキュメントなどにそのまま書くのは避けてください。
- 可能であれば、対面で共有するか、パスワード管理ツールを使ってください。
- 引き継ぎ後は、必要に応じて各サービスのパスワードを変更してください。
- 個人アカウントで新しいSupabase/Netlifyプロジェクトを作らないでください。
- 古い個人アカウント側のNetlify URLは削除済みです。ユーザーに案内するURLは、運営用メール側のNetlify URLだけにしてください。

## 12. 漏らしてはいけない情報

特に重要なのは、Supabaseの `service_role key` です。

漏らしてはいけない情報：

- 運営用メールアドレスのパスワード
- 運営用メールの復旧コード
- Supabase service_role key
- Supabaseログイン情報
- Netlifyログイン情報
- 管理者ページのパスワード
- `supabase.local.json`
- `shift.db`

`service_role key` は、Supabaseのデータを強い権限で操作できる重要な鍵です。
絶対にユーザー向けページ、スクリーンショット、共有チャット、公開フォルダなどに載せないでください。

`supabase.local.json` には `service_role key` が保存されています。
このファイルは管理者だけが持つようにしてください。

一方で、`anon public key` はユーザー向けページで使う公開用のkeyです。
ただし、公開用とはいえ、不要な場所にむやみに貼り付ける必要はありません。

## 13. 引き継ぎ時に渡すファイル

基本的には、以下のフォルダを丸ごと渡すのが安全です。

```text
hirugaku-shift
```

特に重要なファイル：

```text
shift.db
supabase.local.json
static/online-config.js
public/
run.bat
```

注意点：

- `supabase.local.json` には重要なkeyが入っています。
- `shift.db` にはシフトや名簿などの管理データが入っています。
- どちらも管理者以外に共有しないでください。
- ファイル一式に加えて、運営用メールアドレス `hirugaku.shift@gmail.com` のログイン方法も引き継いでください。
- 運営用メールにログインできないと、SupabaseやNetlifyの管理ができません。

## 14. よくある運用手順

希望調査を開始するとき：

1. 月情報を作成・編集する
2. 希望調査を `回答受付中` にする
3. `公開データを送る`
4. 必要に応じて `公開用ファイルを作成`
5. Netlifyへ `public` フォルダをアップロード
6. ユーザーへURLを案内する

回答を確認するとき：

1. `オンライン同期`
2. `回答を取り込む`
3. `回答確認`

シフト表を公開するとき：

1. `シフト作成`
2. 各日付のシフトを保存
3. `希望調査・シフト表の公開設定`
4. シフト表を `公開`
5. `公開データを送る`
6. ユーザー側ページで確認

## 15. トラブル時の確認

ユーザー側に希望調査が表示されない場合：

- 希望調査が `回答受付中` になっているか確認
- `公開データを送る` を実行したか確認
- 必要に応じてNetlifyへ再アップロードしたか確認

回答確認に最新回答が出ない場合：

- `オンライン同期` → `回答を取り込む` を実行
- その後 `回答確認` を開く

シフト表がユーザー側に表示されない場合：

- シフト表が `公開` になっているか確認
- `公開データを送る` を実行したか確認
- Netlifyの公開URLで再読み込みする

管理者ページが開けない場合：

- `run.bat` が起動しているか確認
- URLが `http://127.0.0.1:8000/admin` になっているか確認
- パスワードが正しいか確認

## 16. 基本方針

このシステムでは、以下の流れを守ると安定して運用できます。

```text
管理者がローカルで編集
↓
オンライン同期でSupabaseへ反映
↓
Netlifyでユーザー向けページを公開
↓
ユーザーが回答
↓
管理者が回答を取り込む
↓
シフト作成
↓
シフト表を公開
```

この流れを大きく外さなければ、管理者PCを24時間起動しておく必要はありません。
